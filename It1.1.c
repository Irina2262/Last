#include "drone.h"
#include <stdio.h>

void move_drone(char direction) {
    // Логика перемещения дрона в зависимости от направления
    printf("Дрон перемещается в направлении: %c\n", direction);
}
